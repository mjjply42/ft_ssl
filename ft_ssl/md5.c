#include "ft_ssl.h"
/*unsigned long       padding(ULL pre_pad_size)
{
    unsigned long *after_pad;

//(pre_image->STREAM == TRUE) ? md5_hash(after_image[0]) : md5_hash(after_image[1]);
if ((after_image[0] && after_image[1]) != NULL)
{
    after_image[0] = NULL;
    md5_hash(after_image[1]);
}
return (after_pad[0]);
}
int     md5_start(m_s *pre_image, char **argv)
{
    int i;

    i = 0;
    argv++;
    if (pre_image->stream != NULL)
    {
        pre_image->bit_size = (ft_strlen(pre_image->stream) * sizeof(char));
        padding(pre_image->bit_size);
    }
    if (pre_image->input != NULL)
    {
        pre_image->bit_size = (ft_strlen(pre_image->input) * sizeof(char));
        padding(pre_image->bit_size);
    }
    //destroy_data(pre_image);
    return (pre_image->count);
}*/

/*char        *sha256_start(int argc, char **argv, char *str)
{
    return (NULL);
}*/